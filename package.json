{
  "name": "tw",
  "version": "0.1.0",
  "description": "Minimal CLI for creating and serving Tailwind CSS playgrounds with HMR.",
  "type": "module",
  "bin": {
    "tw": "./bin/tw.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@clack/prompts": "^1.4.0",
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0",
    "vite": "^6.3.0"
  }
}
