{
  "name": "tw",
  "version": "0.2.0",
  "description": "Minimal CLI for creating and serving Tailwind CSS playgrounds with live reload.",
  "type": "module",
  "bin": {
    "tw": "./bin/tw.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@clack/prompts": "^1.4.0",
    "@compodoc/live-server": "^1.2.3"
  }
}
